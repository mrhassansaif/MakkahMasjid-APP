import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
tabLabel: {
    paddingHorizontal: 8,
    color: "#42a8c3",
    fontWeight: 'bold',
},
tabbarContainer: {
    borderRadius: 5
}
})